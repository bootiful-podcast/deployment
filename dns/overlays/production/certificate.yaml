apiVersion: networking.gke.io/v1beta2
kind: ManagedCertificate
metadata:
  name: bootiful-podcast-production-certificate
spec:
  domains:
    - studio.bootifulpodcast.fm
    - api.bootifulpodcast.fm
    - configuration.bootifulpodcast.fm